docker-compose down
>> docker-compose up --build


PARA QUE FUNCIONARA , TUVE QUE CAMBIAR 

EL DATABASE_CONFIG.JSON => LE AÑADI EL CAMPO HOST 
AL MODELO CONFIG => LE AÑADI EL CAMPO HOST
A LA FUNCION CREATECONECCTIONSTRING => LE AÑDI EL CAMPO HOST AL FNAL DEL TOODO , Y EL ARGUMENTO HOST , 

ANTES LA FUNCION ERA ASI : 

func createConnectionString(config models.Config) string {
	return "user=" + config.User + " password=" + config.Password + " dbname=" + config.DBname + " sslmode=disable"
}